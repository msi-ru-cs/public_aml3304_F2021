# Disable sudo to speed up the build
sudo: false

# Set the build language to Python
language: python

# Set the python version to 3.8
python: 3.8

env:
  - CODECOV_TOKEN='760148f0-2830-480c-b0a7-1473979f72e1'

# Install the codecov pip dependency
install:
  - pip install codecov
  - pip install -r requirements.txt

# Run the unit test
script:
  - coverage run pytest.py

# Push the results back to codecov
after_success:
  - codecov


#language: python
#python:
#  - "3.8"
#
#install:
#  - pip install -r requirements.txt
## command to run tests
#script:
#  - python pytest.py

